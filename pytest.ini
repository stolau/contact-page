[pytest]
pythonpath = .
testpaths = tests
markers =
    browser: drives a real headless Google Chrome (tests/browser/). Applied by
        path in tests/browser/conftest.py, never written per file. Run
        `pytest -m "not browser"` for the fast local loop; `pytest` with no
        arguments still runs everything, and CI always does.
